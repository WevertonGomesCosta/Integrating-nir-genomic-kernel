---
title: "Análises de predição - tutorial didático"
author:
  - Costa, W. G.^[Weverton Gomes da Costa, Pós-Doutorando, Departamento de Estatística - Universidade Federal de Viçosa, wevertonufv@gmail.com]
date: "`r Sys.Date()`"
site: workflowr::wflow_site
url: https://wevertongomescosta.github.io/Integrating-nir-genomic-kernel/
output:
  workflowr::wflow_html:
    toc: true
    toc_depth: 3
    toc_float: true
    code_download: true
editor_options:
  chunk_output_type: console
github-repo: WevertonGomesCosta/Integrating-nir-genomic-kernel
---

**Language / Idioma:** [English](analysis_prediction.html) | [Português](analysis_prediction_pt.html)

> **Nota operacional:** Nesta versão em português, a etapa pesada de predição permanece documentada, mas a renderização depende dos objetos já gerados nas etapas anteriores do pipeline e não reroda os ajustes Bayesianos completos. Os chunks de carregamento podem ficar ocultos e o ajuste completo só deve ser executado na versão em inglês quando for necessário atualizar os resultados.

## 1. Introdução

Este tutorial realiza as análises de predição para o conjunto reduzido de 18 modelos Bayesianos multi-kernel usados no estudo.

O script combina:

- kernels genômicos,
- kernels fenômicos,
- o efeito categórico de ambiente (`E`),
- e o kernel climático (`W`).

Neste fluxo, o kernel climático é usado principalmente nos modelos **M10-M18**. Ele deve ser interpretado como uma fonte de similaridade derivada do clima que complementa, mas não substitui, o efeito categórico de ambiente.

O componente fenômico (`P`) também deve ser interpretado com cautela. A similaridade baseada em NIR pode capturar estrutura preditiva útil, mas também pode refletir forte resposta fisiológica ao ambiente em vez de apenas similaridade biológica estável.

Um modelo que apenas se ajusta bem aos dados observados não é suficiente; ele precisa ser capaz de **prever dados que nunca viu**. Para simular isso, usamos Validação Cruzada (CV).

Este script implementa múltiplos esquemas de CV para simular diferentes desafios do mundo real no melhoramento:

- **CV1: validação por genótipo (testa genótipos não observados).**
    - *Pergunta:* O modelo consegue prever o desempenho de um genótipo (híbrido) *completamente novo*, que não estava presente em *nenhum* ambiente de treinamento?
- **CV2: validação por célula (testa combinações genótipo × ambiente parcialmente observadas).**
    - *Pergunta:* O modelo consegue prever o desempenho de um genótipo *conhecido* em um ambiente que é *novo para esse genótipo* (assumindo que o genótipo foi observado em outros ambientes e que o ambiente foi observado com outros genótipos)?
- **CV0: leave-one-environment-out (testa ambientes inteiramente não observados).**
    - *Pergunta:* O teste mais difícil. O modelo consegue prever o desempenho de *todos* os genótipos em um *ambiente completamente novo* (um ano ou local que o modelo nunca viu)?
- **CV00: leave-one-environment-out (testa genótipos em ambientes não observados).**
    - *Semelhante ao CV0*, mas focado em prever genótipos *dentro* de um ambiente deixado de fora.

As seções restantes mostram como esses esquemas de validação são operacionalizados no fluxo de predição, como os jobs são distribuídos em paralelo e como as saídas são gravadas para a etapa de visualização a jusante em `visualization.Rmd` e `visualization.html`.

## Entradas, saídas e próxima etapa

**Entradas desta etapa**
- matrizes e kernels gerados em `matrizes.Rmd`
- o conjunto reduzido de 18 modelos de predição adotado no projeto

**Saídas desta etapa**
- configurações gerais da etapa de predição
- catálogo estruturado de modelos
- especificação dividida de `Eta_list`
- desenho conceitual de `CV1`, `CV2`, `CV0` e `CV00`

**Próxima etapa**
- `analysis_prediction_run_outputs.Rmd` documenta a execução operacional dos jobs de predição e a organização das saídas usadas depois por `visualization.Rmd`.

## 2. Pacotes e opções

```{r setup}
rm(list = ls())
options(scipen = 999)

library(BGLR)
library(dplyr)
library(readr)
library(stringr)
library(foreach)
library(doParallel)
library(parallel)
```

## 3. Entradas geradas pelos scripts anteriores

```{r load-inputs, include=FALSE}
Pheno <- readRDS("output/Matrizes/Pheno.rds")

ZG   <- readRDS("output/Matrizes/ZG.rds")
ZP   <- readRDS("output/Matrizes/ZP.rds")
ZE   <- readRDS("output/Matrizes/ZE.rds")
ZW   <- readRDS("output/Matrizes/ZW.rds")

ZGZE <- readRDS("output/Matrizes/ZGZE.rds")
ZPZE <- readRDS("output/Matrizes/ZPZE.rds")
ZGZW <- readRDS("output/Matrizes/ZGZW.rds")
ZPZW <- readRDS("output/Matrizes/ZPZW.rds")

GGK  <- readRDS("output/Matrizes/GGK.rds")
PGK  <- readRDS("output/Matrizes/PGK.rds")
GAK  <- readRDS("output/Matrizes/GAK.rds")
PAK  <- readRDS("output/Matrizes/PAK.rds")

GGKE <- readRDS("output/Matrizes/GGKE.rds")
PGKE <- readRDS("output/Matrizes/PGKE.rds")
GAKE <- readRDS("output/Matrizes/GAKE.rds")
PAKE <- readRDS("output/Matrizes/PAKE.rds")

GGKW <- readRDS("output/Matrizes/GGKW.rds")
PGKW <- readRDS("output/Matrizes/PGKW.rds")
GAKW <- readRDS("output/Matrizes/GAKW.rds")
PAKW <- readRDS("output/Matrizes/PAKW.rds")
```

Os arquivos atuais `matrizes.Rmd` e `matrizes.html` salvam as matrizes em nível de observação usando os seguintes nomes:

- `ZE` para a matriz de desenho categórica de ambiente
- `ZGZE` e `ZPZE` para interações genômico-por-ambiente e fenômico-por-ambiente
- `ZGZW` e `ZPZW` para interações genômico-por-clima e fenômico-por-clima

Por essa razão, este script de predição lê essas matrizes diretamente dos arquivos produzidos por `matrizes.Rmd`.

## 4. Configurações de MCMC e paralelização

```{r mcmc-settings}
nIter <- 10000
burnIn <- 1000
thin <- 10
num_rep <- 20

n_cores <- max(1, parallel::detectCores() - 1)

nIter
burnIn
thin
num_rep
n_cores
```

## 5. Catálogo de modelos

```{r model-catalog}
model_catalog <- data.frame(
  ModelNumber = 1:18,
  Model = sprintf("M%02d", 1:18),
  Description = c(
    "E + G + GE",
    "E + P + PE",
    "E + GGK + GGKE",
    "E + PGK + PGKE",
    "E + GAK + GAKE",
    "E + PAK + PAKE",
    "E + G + P + GE + PE",
    "E + GGK + PGK + GGKE + PGKE",
    "E + GAK + PAK + GAKE + PAKE",
    "E + W + G + GE + GW",
    "E + W + P + PE + PW",
    "E + W + G + P + GE + PE + GW + PW",
    "E + W + GGK + GGKE + GGKW",
    "E + W + PGK + PGKE + PGKW",
    "E + W + GGK + PGK + GGKE + PGKE + GGKW + PGKW",
    "E + W + GAK + GAKE + GAKW",
    "E + W + PAK + PAKE + PAKW",
    "E + W + GAK + PAK + GAKE + PAKE + GAKW + PAKW"
  ),
  stringsAsFactors = FALSE
)

model_catalog
```

## 6. Especificação dos modelos

```{r eta-list}
Eta_list <- list()

Eta_list[[1]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZG,   model = "RKHS"),
                       list(K = ZGZE, model = "RKHS"))

Eta_list[[2]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZP,   model = "RKHS"),
                       list(K = ZPZE, model = "RKHS"))

Eta_list[[3]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = GGK,  model = "RKHS"),
                       list(K = GGKE, model = "RKHS"))

Eta_list[[4]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = PGK,  model = "RKHS"),
                       list(K = PGKE, model = "RKHS"))

Eta_list[[5]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = GAK,  model = "RKHS"),
                       list(K = GAKE, model = "RKHS"))

Eta_list[[6]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = PAK,  model = "RKHS"),
                       list(K = PAKE, model = "RKHS"))

Eta_list[[7]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZG,   model = "RKHS"),
                       list(K = ZP,   model = "RKHS"),
                       list(K = ZGZE, model = "RKHS"),
                       list(K = ZPZE, model = "RKHS"))

Eta_list[[8]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = GGK,  model = "RKHS"),
                       list(K = PGK,  model = "RKHS"),
                       list(K = GGKE, model = "RKHS"),
                       list(K = PGKE, model = "RKHS"))

Eta_list[[9]]  <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = GAK,  model = "RKHS"),
                       list(K = PAK,  model = "RKHS"),
                       list(K = GAKE, model = "RKHS"),
                       list(K = PAKE, model = "RKHS"))

Eta_list[[10]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = ZG,   model = "RKHS"),
                       list(K = ZGZE, model = "RKHS"),
                       list(K = ZGZW, model = "RKHS"))

Eta_list[[11]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = ZP,   model = "RKHS"),
                       list(K = ZPZE, model = "RKHS"),
                       list(K = ZPZW, model = "RKHS"))

Eta_list[[12]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = ZG,   model = "RKHS"),
                       list(K = ZP,   model = "RKHS"),
                       list(K = ZGZE, model = "RKHS"),
                       list(K = ZPZE, model = "RKHS"),
                       list(K = ZGZW, model = "RKHS"),
                       list(K = ZPZW, model = "RKHS"))

Eta_list[[13]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = GGK,  model = "RKHS"),
                       list(K = GGKE, model = "RKHS"),
                       list(K = GGKW, model = "RKHS"))

Eta_list[[14]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = PGK,  model = "RKHS"),
                       list(K = PGKE, model = "RKHS"),
                       list(K = PGKW, model = "RKHS"))

Eta_list[[15]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = GGK,  model = "RKHS"),
                       list(K = PGK,  model = "RKHS"),
                       list(K = GGKE, model = "RKHS"),
                       list(K = PGKE, model = "RKHS"),
                       list(K = GGKW, model = "RKHS"),
                       list(K = PGKW, model = "RKHS"))

Eta_list[[16]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = GAK,  model = "RKHS"),
                       list(K = GAKE, model = "RKHS"),
                       list(K = GAKW, model = "RKHS"))

Eta_list[[17]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = PAK,  model = "RKHS"),
                       list(K = PAKE, model = "RKHS"),
                       list(K = PAKW, model = "RKHS"))

Eta_list[[18]] <- list(list(X = model.matrix(~ Pheno$Env - 1), model = "BRR"),
                       list(K = ZW,   model = "RKHS"),
                       list(K = GAK,  model = "RKHS"),
                       list(K = PAK,  model = "RKHS"),
                       list(K = GAKE, model = "RKHS"),
                       list(K = PAKE, model = "RKHS"),
                       list(K = GAKW, model = "RKHS"),
                       list(K = PAKW, model = "RKHS"))
```

## Navegação entre etapas

**Etapa anterior:** [Componentes de variância](variance_components_pt.html)  
**Próxima etapa:** [Predição — Parte II](analysis_prediction_run_outputs_pt.html)
